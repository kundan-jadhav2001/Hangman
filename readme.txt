First download dependencies by typing "pip install django" and "pip install smtplib" in terminal.
then open folder in VS Code or open terminal in the folder
type "python manage.py runserver" which will give a url
type that url in browser in the and hit enter
that will run the website
